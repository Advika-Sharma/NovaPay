version: 2

models:
  - name: stg_crm__customers
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
          
  - name: stg_crm__customer_interactions
    columns:
      - name: interaction_sk
        tests:
          - unique
          - not_null
      - name: customer_id
        tests:
          - relationships:
              to: ref('stg_crm__customers')
              field: customer_id